<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="../main.css">
    <title>Hangman</title>
  </head>
  <body>
    <nav>
      <div class="menu">
      </div>
    </nav>

    <main>

      <div class="keyboard">
        <% for (i = 0; i < alphabet.length; i++) {
          if (guesses > 0) { %>
            <a href="/<%= i %>" class="key"><%=alphabet[i]%></a>
          <% }
          else { %>
            <div class="key"><%=alphabet[i]%></div>
          <% }
        } %>
      </div>

      <div class="word">
        <input type="text" class="word-space">
        </input>
        <div class="word-button">Guess word
        </div>
      </div>

      <div class="blanks">
        <% for (i = 0; i < currentWord.length; i++) { %>
          <div class="blank">
            <% if (correctLetters.includes(currentWord[i])) { %>
              <%= currentWord[i] %>
            <% } %>
          </div>
        <% } %>
      </div>


      <div class="play-again">

        <% let count = 0;
        for (i = 0; i < currentWord.length; i++) {
          console.log(correctLetters.indexOf(currentWord[i]));
          if (correctLetters.indexOf(currentWord[i]) >= 0) {
            console.log(correctLetters.indexOf(currentWord[i]));
            count++;
          };
        };
        console.log('count:', count);

        if (count === currentWord.length) { %>
          <div>Great job! Play again?</div>
          <a href="/" class="word-button">Yes</a>
          <div class="word-button">No</div>
        <% }
        else if (guesses === 0) { %>
          <div>You lost :(</div>
          <a href="/" class="word-button">Start over</a>
        <% }
        else { %>
          <%=guesses%> guesses left!
        <% } %>

      </div>
    </main>

  </body>
</html>
